<!DOCTYPE html>
<html>
    <head>
        {% include 'html/head.liquid' %}
        <title>{{ title }}</title>
    </head>
    <body>
        <div class="container">
            <div class="content">
                {{ content }}
            </div>
        </div>
    </body>
</html>
